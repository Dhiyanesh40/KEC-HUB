import React from 'react';
import { Link } from 'react-router-dom';

const LoginPage: React.FC<{ onLoginSuccess: (userData: any) => void }> = ({ onLoginSuccess }) => {
  return <div>Login Page - Import AuthPage component</div>;
};

export default LoginPage;
