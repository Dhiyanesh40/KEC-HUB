import React from 'react';

const RegisterPage: React.FC<{ onLoginSuccess: (userData: any) => void }> = ({ onLoginSuccess }) => {
  return <div>Register Page - Import AuthPage component</div>;
};

export default RegisterPage;
